import 'package:flutter/material.dart';

class VoiceScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Голосовой режим')),
      body: Center(child: Text('Голосовой ассистент здесь')),
    );
  }
}
